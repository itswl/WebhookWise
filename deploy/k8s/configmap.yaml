apiVersion: v1
kind: ConfigMap
metadata:
  name: webhookwise-config
  labels:
    app.kubernetes.io/name: webhookwise
data:
  APP_ENV: production
  APP_VERSION: "3.2.0"
  HOST: 0.0.0.0
  PORT: "8000"
  LOG_LEVEL: INFO
  REQUIRE_WEBHOOK_AUTH: "true"
  REDIS_URL: redis://webhookwise-redis:6379/0
  # Leaving OTEL_ENABLED "true" with a blank OTEL_EXPORTER_OTLP_ENDPOINT makes
  # telemetry a silent no-op (dashboards stay empty), so it defaults to "false"
  # to match the compose stack. Set both this to "true" and the endpoint to your
  # cluster collector before rollout.
  OTEL_ENABLED: "false"
  OTEL_EXPORTER_OTLP_ENDPOINT: ""
  OTEL_EXPORTER_OTLP_PROTOCOL: grpc
  OTEL_SERVICE_NAMESPACE: webhookwise
  OTEL_SERVICE_VERSION: "3.2.0"
  OTEL_SCHEMA_URL: https://opentelemetry.io/schemas/1.41.0
  OTEL_SEMCONV_VERSION: "1.41.0"
  OTEL_SEMCONV_STABILITY_OPT_IN: http
  OTEL_LOGS_ENABLED: "true"
  OTEL_METRICS_EXEMPLAR_FILTER: trace_based
  OTEL_METRIC_EXPORT_INTERVAL: "10000"
  OTEL_TRACES_SAMPLER: parentbased_traceidratio
  OTEL_TRACES_SAMPLER_ARG: "0.1"
  DB_POOL_SIZE: "5"
  DB_MAX_OVERFLOW: "5"
  DB_STATEMENT_TIMEOUT_MS: "30000"
  BACKGROUND_SCAN_INTERVAL_SECONDS: "300"
  FORWARD_OUTBOX_STALE_SECONDS: "300"
  PROCESSING_LOCK_DISTRIBUTED_ENABLED: "true"
  PROCESSING_LOCK_TTL_SECONDS: "180"
  PROCESSING_LOCK_WAIT_TIMEOUT_SECONDS: "15"
  PROCESSING_LOCK_POLL_INTERVAL_MS: "100"
  PROCESSING_LOCK_FAILFAST_THRESHOLD: "20"
  PROCESSING_LOCK_FAILFAST_WINDOW_SECONDS: "10"
