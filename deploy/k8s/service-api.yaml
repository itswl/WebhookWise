apiVersion: v1
kind: Service
metadata:
  name: webhookwise-api
  labels:
    app.kubernetes.io/name: webhookwise
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: webhookwise
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
